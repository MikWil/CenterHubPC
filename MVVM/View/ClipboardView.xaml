<UserControl x:Class="CenterHubNew.MVVM.View.ClipboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CenterHubNew.MVVM.View"
             xmlns:viewmodel="clr-namespace:CenterHubNew.MVVM.ViewModel"
             d:DataContext="{d:DesignInstance Type=viewmodel:ClipboardViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="1200"
             Background="Transparent"
             FontFamily="Segoe UI">
    
    <UserControl.Resources>
        <local:BoolToMonitoringTextConverter x:Key="BoolToMonitoringTextConverter"/>
        <local:BoolToPinBorderConverter x:Key="BoolToPinBorderConverter"/>
    </UserControl.Resources>
    
    <Grid Margin="20,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header Row -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <TextBlock Text="📋" FontSize="20" Margin="0,0,8,0" Foreground="{StaticResource AccentLightBrush}"/>
                <TextBlock Text="Clipboard History" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="{Binding IsMonitoring, Converter={StaticResource BoolToMonitoringTextConverter}}"
                        Style="{StaticResource SecondaryButton}" Padding="12,6" FontSize="11"
                        Command="{Binding ToggleMonitoringCommand}"/>
                <Button Content="Clear" Style="{StaticResource SecondaryButton}" Padding="12,6" FontSize="11" Margin="8,0,0,0"
                        Command="{Binding ClearHistoryCommand}"/>
            </StackPanel>
        </Grid>
        
        <!-- History List -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding ClipboardHistory}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border x:Name="Item" Background="{StaticResource CardInnerBrush}" CornerRadius="6" Padding="10,8" Margin="0,0,0,4"
                                    BorderBrush="{Binding IsPinned, Converter={StaticResource BoolToPinBorderConverter}}" BorderThickness="1" Cursor="Hand">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Preview}" TextTrimming="CharacterEllipsis" FontSize="12" 
                                                   Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Left"/>
                                        <TextBlock FontSize="10" Foreground="{StaticResource TextMutedBrush}" Margin="0,3,0,0" HorizontalAlignment="Left">
                                            <Run Text="{Binding Timestamp, StringFormat='{}{0:HH:mm}', Mode=OneWay}"/>
                                            <Run Text=" · "/>
                                            <Run Text="{Binding Content.Length, StringFormat='{}{0} chars', Mode=OneWay}"/>
                                        </TextBlock>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                        <Button ToolTip="Pin" Background="Transparent" BorderThickness="0" Padding="6,3" Cursor="Hand"
                                                Command="{Binding DataContext.TogglePinCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}">
                                            <TextBlock Text="📌" FontSize="12" Foreground="{StaticResource AccentLightBrush}"/>
                                        </Button>
                                        <Button ToolTip="Copy" Background="Transparent" BorderThickness="0" Padding="6,3" Cursor="Hand"
                                                Command="{Binding DataContext.CopyToClipboardCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}">
                                            <TextBlock Text="📄" FontSize="12" Foreground="{StaticResource AccentLightBrush}"/>
                                        </Button>
                                        <Button ToolTip="Delete" Background="Transparent" BorderThickness="0" Padding="6,3" Cursor="Hand"
                                                Command="{Binding DataContext.DeleteItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}">
                                            <TextBlock Text="🗑️" FontSize="12" Foreground="{StaticResource ErrorBrush}"/>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </Border>
                            <DataTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Item" Property="Background" Value="{StaticResource InputBackgroundBrush}"/>
                                </Trigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
