<UserControl x:Class="CenterHubNew.MVVM.View.ComputerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodel="clr-namespace:CenterHubNew.MVVM.ViewModel"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400"
             Background="#232136">
    <UserControl.Resources>
        <Color x:Key="AccentColor">#7F5AF0</Color>
        <Color x:Key="CardBackground">#272537</Color>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="CardBackgroundBrush" Color="{StaticResource CardBackground}"/>
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Margin" Value="0,8,0,0"/>
            <Setter Property="Padding" Value="12,4"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="8,2"/>
            <Setter Property="Margin" Value="0,8,0,0"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#353340"/>
            <Setter Property="Foreground" Value="#CFCFCF"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Border Background="{StaticResource CardBackgroundBrush}" CornerRadius="18" Padding="32" Margin="24">
            <Border.Effect>
                <DropShadowEffect Color="#000" BlurRadius="8" ShadowDepth="1" Opacity="0.18"/>
            </Border.Effect>
            <StackPanel>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,18">
                    <TextBlock Text="💻" FontSize="22" Margin="0,0,8,0"/>
                    <TextBlock Text="Computer Shutdown Scheduler" FontSize="22" FontWeight="Bold"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                    <TextBlock Text="Shutdown at:" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <DatePicker SelectedDate="{Binding SelectedDate, Mode=TwoWay}" Width="120"/>
                    <TextBox Text="{Binding SelectedTime, Mode=TwoWay}" Width="80" Margin="8,0,0,0"/>
                </StackPanel>
                <Button Content="Schedule Shutdown" Command="{Binding ScheduleShutdownCommand}" Width="180"/>
                <Button Content="Cancel Shutdown" Command="{Binding CancelShutdownCommand}" Width="180" Background="#FF5A5A68" Margin="0,8,0,0"/>
                <TextBlock Text="{Binding StatusMessage}" Margin="0,12,0,0" FontSize="14"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl> 