<UserControl x:Class="CenterHubNew.MVVM.View.StandingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CenterHubNew.MVVM.View"
             xmlns:viewmodel="clr-namespace:CenterHubNew.MVVM.ViewModel"
             d:DataContext="{d:DesignInstance Type=viewmodel:StandingViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="1200"
             Background="Transparent"
             FontFamily="Segoe UI">
    
    <Grid Margin="20,16">
        <StackPanel>
            <!-- Header -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,14">
                <TextBlock Text="🧍" FontSize="20" Margin="0,0,8,0" Foreground="{StaticResource AccentLightBrush}"/>
                <TextBlock Text="Standing Timer" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
            </StackPanel>
            
            <!-- Timer Settings -->
            <Border Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Inputs Row -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                        <TextBlock Text="Sit" VerticalAlignment="Center" FontSize="12" Margin="0,0,8,0" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <TextBox Text="{Binding SittingMinutes, UpdateSourceTrigger=PropertyChanged}" 
                                 Style="{StaticResource InputTextBox}" Width="80"/>
                        <TextBlock Text="min" VerticalAlignment="Center" FontSize="11" Margin="6,0,24,0" Foreground="{StaticResource TextMutedBrush}"/>
                        <TextBlock Text="Stand" VerticalAlignment="Center" FontSize="12" Margin="0,0,8,0" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <TextBox Text="{Binding StandingMinutes, UpdateSourceTrigger=PropertyChanged}" 
                                 Style="{StaticResource InputTextBox}" Width="80"/>
                        <TextBlock Text="min" VerticalAlignment="Center" FontSize="11" Margin="6,0,0,0" Foreground="{StaticResource TextMutedBrush}"/>
                    </StackPanel>
                    
                    <!-- Buttons Row -->
                    <StackPanel Grid.Row="1" Orientation="Horizontal">
                        <Button Content="Start" Style="{StaticResource PrimaryButton}" Padding="24,8"
                                Command="{Binding StartTimersCommand}"
                                IsEnabled="{Binding IsStartButtonEnabled}" Margin="0,0,8,0"/>
                        <Button Content="Stop" Style="{StaticResource SecondaryButton}" Padding="24,8"
                                Command="{Binding StopTimersCommand}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
