<UserControl x:Class="CenterHubNew.MVVM.View.StandingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CenterHubNew.MVVM.View"
             xmlns:viewmodel ="clr-namespace:CenterHubNew.MVVM.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="350"
             Background="#232136">
    <UserControl.Resources>
        <Color x:Key="AccentColor">#7F5AF0</Color>
        <Color x:Key="CardBackground">#272537</Color>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="CardBackgroundBrush" Color="{StaticResource CardBackground}"/>
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Margin" Value="0,8,0,0"/>
            <Setter Property="Padding" Value="12,4"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="8,2"/>
            <Setter Property="Margin" Value="0,8,0,0"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#353340"/>
            <Setter Property="Foreground" Value="#CFCFCF"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Border Background="{StaticResource CardBackgroundBrush}" CornerRadius="18" Padding="32" Margin="24" >
            <Border.Effect>
                <DropShadowEffect Color="#000" BlurRadius="8" ShadowDepth="1" Opacity="0.18"/>
            </Border.Effect>
            <StackPanel>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,18">
                    <TextBlock Text="🧍" FontSize="22" Margin="0,0,8,0"/>
                    <TextBlock Text="Standing Timer" FontSize="22" FontWeight="Bold"/>
                </StackPanel>
                <Grid Margin="0,0,0,18">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Sitting minutes:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <TextBox Text="{Binding SittingMinutes, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="1"/>
                    <TextBlock Text="Standing minutes:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <TextBox Text="{Binding StandingMinutes, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="1"/>
                </Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,0">
                    <Button Content="Start" Width="100" Height="32" Margin="0,0,12,0" IsEnabled="{Binding IsStartButtonEnabled}" Command="{Binding StartTimersCommand}"/>
                    <Button Content="Stop" Width="100" Height="32" Margin="0,0,12,0" Command="{Binding StopTimersCommand}" Background="#FF5A5A68"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
